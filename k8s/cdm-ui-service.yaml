# cdm-ui-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: cdm-ui-service
  namespace: cdm
  labels:
    app: cdm-ui
    component: frontend
spec:
  selector:
    app: cdm-ui
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
