apiVersion: v1
kind: Secret
metadata:
  name: wildcard-endusercompute-tls
type: kubernetes.io/tls
data:
  # You'll need to replace these with your actual base64 encoded certificate and key
  tls.crt: ${BASE64_ENCODED_CERT}
  tls.key: ${BASE64_ENCODED_KEY}
